# 그라파나 기동 시 prometheus 데이터소스를 자동 등록한다(UI 에서 수동 추가 불필요).
apiVersion: 1

datasources:
  - name: Prometheus
    # uid 를 고정해야 provisioning 으로 올리는 대시보드 JSON 이 데이터소스를 참조할 수 있다.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
