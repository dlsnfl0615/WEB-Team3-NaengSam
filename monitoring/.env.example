# 모니터링 스택 전용 환경변수. 배포 서버의 monitoring/ 폴더에 .env 로 복사해서 채운다.
# 백엔드의 .env 와는 별개 파일이다.
#
# 기본값 admin/admin 을 그대로 두면 안 된다 — Grafana 는 3000 포트가 외부에 열려 있다.
GF_SECURITY_ADMIN_USER=
GF_SECURITY_ADMIN_PASSWORD=
