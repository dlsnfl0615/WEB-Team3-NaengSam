/** 픽업지/배달 도착지에서 이 거리(m)를 초과하면 완료 전 경고한다. */
export const DELIVERY_COMPLETION_WARNING_DISTANCE_M = 200;

/** 배달 지도 드리미 핀에 GPS 흔들림 억제와 GPU 보간 이동을 적용할지 여부. */
export const DELIVERY_MAP_SMOOTH_MODE = true;
