# API 오리진. 개발은 비워두면 동일 출처(`/api/v1` → Vite 프록시 → localhost:8080)로 요청된다.
# 운영에서는 API 오리진(예: https://api.example.com)을 지정한다. 경로 `/api/v1`은 생성 클라이언트가 포함한다.
VITE_API_BASE_URL=

# 카카오 JavaScript 앱키(지도 표시용). 비워두면 지도만 폴백, 주소 검색·등록은 정상 동작.
VITE_KAKAO_MAP_KEY=
